---
name: iny-build
description: 05단계. 04 가 그린 화면설계서를 받아 실제로 사이트를 짓고 올리고 검사한다. 주소 규칙을 고르고, 글과 사진을 어디에 담을지 짓기 전에 정하고, 요소마다 적힌 다섯 층을 그대로 코드로 옮기고, 04 가 정한 기계가 읽는 표시를 붙이고, 로봇 파일과 사이트맵을 만든다. 앞 화면을 먼저 세워 올린 뒤 실제로 받아서 재고, 그다음 관리 화면을 붙인다. 파일만 보고 통과시키지 않고 배포한 주소를 받아서 앞단에 막힌 데가 없는지 확인한다. 관리 화면은 막혔는지만 보지 않고 안이 그려지는지와 저장이 실제로 되는지까지 잰다. 검사표를 채워서 낸다. 여기서 선 사이트에 06 이 첫 글을 낸다. 트리거는 "사이트 짓기", "사이트 만들기", "빌드", "배포", "사이트 올리기", "홈페이지 만들어줘", "iny-build", "05 시작".
---

# 05 BUILD · 사이트 짓기

`v1.1`

백지에서 쓴다. 남의 저장소에서 코드를 가져오지 않는다.
판단 근거는 [`references/근거와한계.md`](references/근거와한계.md) 맨 끝에 적어 뒀다.

## ⚑ 실행 순서

건너뛰지 않는다.

| STEP | 무엇 | 멈추나 |
|---|---|---|
| **1** | **안내문을 낸다** (아래 「첫 메시지」 그대로) | ✋ 답할 때까지 |
| **2** | 04·03·02 산출물을 읽는다. 없으면 04 부터 하시라고 안내하고 멈춘다 | |
| **3** | **주소를 어떻게 만들지** · 클릭 ① | ✋ |
| **4** | **어디에 올릴지** · 클릭 ② · **글과 사진을 어디에 담을지** · 클릭 ③ | ✋ |
| **5** | **앞 화면을 짓는다.** 디자인 값 · 내용 채우기 · 표시 · 로봇 · 사이트맵 | |
| **6** | **올린다** | |
| **7** | **★ 받아서 잰다.** 검사표를 채운다 | |
| **8** | **✕ 를 고칠지** · 클릭 ④ | ✋ |
| **9** | **주소를 지금 살지** · 클릭 ⑤ | ✋ |
| **10** | **관리 화면을 짓는다.** 동작까지. 그리고 **막는다** | |
| **11** | **다시 올리고 다시 받아본다.** ✕ 가 0이 될 때까지 | |
| **12** | **문서를 쓰고 `plan.md` 와 06 안내를 한 덩어리로 낸다** | ✋ 마지막 |

**STEP 1 을 건너뛰지 않는다.** "사이트 짓기" 라고만 해도 안내문부터 낸다.
설정이나 04 산출물부터 읽고 그 상태를 보고하지 않는다.

## ⚑ ★ 앞 화면을 먼저 세운다. 한 번에 다 짓지 않는다

**05 는 앞 넷보다 훨씬 크다.** 화면도 많고 관리 화면도 있고 올리는 일까지 있다.

> **한 번에 다 지으면 중간에 끊겼을 때 아무것도 안 남는다.**

```
앞 화면을 짓는다 → 올린다 → 받아서 잰다 → 고친다 → 그다음 관리 화면
```

| | |
|---|---|
| 앞 화면까지만 돼도 | **사이트가 서 있다.** 사람이 볼 수 있다 |
| 관리 화면에서 막혀도 | **되돌아갈 자리가 있다** |
| 일찍 재면 | **앞단 함정을 먼저 잡는다.** 다 짓고 나서 잡으면 다시 짓는다 |

**STEP 7 을 뒤로 미루지 않는다.** 올리자마자 잰다.

## ⚑ 꼬리를 빠뜨리지 않는다

**앞 회차에 마지막 두 STEP 이 통째로 날아간 적이 있다.** 확인받는 자리에서 대화가 끝났고
그 뒤에 있던 것이 전부 밀렸다.

> **확인받는 자리 뒤에 뭘 두지 않는다. 거기서 세션이 끝난다.**

| | |
|---|---|
| 1 | **STEP 12 가 마지막이다.** 문서 쓰기 · `plan.md` 보여주기 · 06 안내를 **한 덩어리로** 낸다 |
| 2 | 그 덩어리의 **맨 끝**이 "고칠 데 말씀해 주세요" 다. 그 뒤에 아무것도 없다 |
| 3 | **`plan.md` 「다음에 할 일」에 이 스킬이 할 일을 적지 않는다** |
| 4 | 산출물에 **「나중에」 · 「필요하면」 · 「사용자가 직접」** 을 쓰지 않는다 |

## ⚑ ★ 05 는 「아무것도 안 깝니다」를 못 쓴다

**앞 넷과 여기가 제일 크게 다르다.**

| | |
|---|---|
| 01~04 | **아무것도 안 깔았다.** 문서만 만들었다 |
| **05** | **짓는 도구가 필요하다.** 안 깔면 사이트가 안 선다 |

> **앞 단계의 약속을 그대로 옮기면 거짓말이 된다.**

**대신 셋을 말한다.**

| | |
|---|---|
| 1 | **무엇을 왜 까는지** |
| 2 | **어디에 깔리는지** (작업 폴더 안인지 컴퓨터 전체인지) |
| 3 | **지우려면 어떻게 하는지** |

**3번을 반드시 적는다.** 산출물 `지은것.md` 에 지우는 법이 없으면 그 문서는 미완성이다.

**그리고 필요 없는 걸 안 깐다.** 아래 「없는 복잡함」을 본다.

## ⚑ ★ 없는 복잡함을 준비하지 않는다

**04 산출물을 열어보면 `API` 층이 거의 다 「없음」이다.** 세어보고 시작한다.

| | |
|---|---|
| 무슨 뜻인가 | **바깥 서비스와 주고받을 게 거의 없다** |
| 그래서 없는 것 | 열쇠를 넣을 자리 · 실패를 다룰 자리 · 값이 새는 자리 |

**05 가 짓는 것은 대개 「읽기만 하는 사이트」다.** 글을 읽어와 화면에 놓는 것이 전부다.

> **이걸 모르고 시작하면 인증 · 데이터베이스 · 상태관리를 앞세운다.
> 그러면 개인 사이트가 안 선다.**

**`Logic` 이 「없음」이 아닌 자리만 동작이 있다.** 거기만 만든다.
**`API` 가 「없음」이 아닌 자리가 있으면 그것만 따로 다룬다.** 대개 연락 칸이다.

## ⚑ ★ 파일을 보는 것과 받아보는 것은 다르다

**05 가 걸리는 자리는 「못 지어서」가 아니라 「올린 뒤에 조용히 어긋나서」다.**

| | |
|---|---|
| 파일 안 | 다 맞게 적혀 있다 |
| **받아보면** | **앞단에서 막혀 있거나, 올린 것으로 지정 안 된 주소이거나** |

**앞 넷은 산출물이 파일이라 틀리면 그 자리에서 보였다. 05 는 아니다.**

```bash
bash "{이 스킬이 설치된 폴더}/scripts/받아보기.sh" {올린 주소} --봇
```

**맥에 이미 있는 것만 쓴다.** 아무것도 더 안 깐다.

| 스크립트가 말하는 것 | 05 가 할 일 |
|---|---|
| `○` | 검사표에 그대로 옮긴다 |
| `✕` | **고친다.** 못 고치면 왜 못 고치는지 적는다 |
| `✕ 검색 금지 머리글` | ★ **올린 것으로 지정된 주소가 맞는지 먼저 본다** |
| `✕ 로봇이 막힙니다` | ★ **올린 곳의 방화벽·봇 관리 설정을 본다** |

> **★ 결과를 짐작해서 적지 않는다.** 받아본 것만 적는다.
> 스크립트가 안 돌면 직접 받아보고 **직접 받아봤다고 적는다.**

## ⚑ ★ 멀쩡해 보이는데 틀린 것을 잡는다

**05 에서 제일 비싼 실수는 화면이 안 뜨는 게 아니다. 화면은 멀쩡한데 값이 옛것인 것이다.**

안 뜨면 그 자리에서 안다. 옛 값이 나오면 아무도 모른다.

### 굳혀두는 층이 몇 겹인지 세고 짓는다

1회차에서 층이 **넷**이었다. 세지 않고 지어서 두 번 헛짚었다.

| 층 | 누가 갖고 있나 |
|---|---|
| 1 | 짓는 도구가 가져온 값을 갖고 있다 |
| 2 | 짓는 도구가 만든 화면을 통째로 갖고 있다 |
| 3 | 올린 곳 앞단이 내준 화면을 또 갖고 있다 |
| 4 | **담을 데가 지역마다 응답을 따로 갖고 있다** |

**4번을 아무도 안 센다.** 담을 데는 저장하는 곳이라 굳힌다고 생각을 안 한다.
1회차에서 내 컴퓨터에서는 새 값이 오는데 올린 곳에서는 옛 값이 왔고, **그동안 오류가 하나도 안 났다.**

> **글이 자주 바뀌는 사이트는 처음부터 안 굳히는 쪽으로 짓는다.**
> 손님이 늘면 그때 굳힌다. 08 이 재 준다. **반대 순서로 가면 오늘 같은 일을 겪는다.**

### 실패를 조용히 삼키지 않는다

**떨어질 데를 만들되, 떨어질 때 반드시 소리를 낸다.**

1회차에서 「담을 데를 못 읽으면 조용히 파일로 간다」고 지었다.
그래서 못 읽는 동안 화면이 멀쩡히 떠 있었고, **무엇이 틀렸는지 아무도 몰랐다.**

| 안 되는 것 | 되는 것 |
|---|---|
| `catch {}` 로 넘어간다 | `catch` 에서 **무엇을 왜 못 읽었는지 남긴다** |
| 「어차피 떨어질 데가 있으니까」 | 떨어졌다는 사실 자체가 **고쳐야 할 일**이다 |

## ⚑ ★ 껐다 켤 수 있는 값을 제목으로 쓰지 않는다

**켜고 끄는 스위치가 달린 값은 화면의 뼈대가 될 수 없다.**

1회차에서 컨택 화면의 큰제목이 **메일 주소 그 자체**였다.
사용자가 메일을 끄자 그 화면에 큰제목이 하나도 없어졌다. 감사에서 ✕ 가 났다.

| | |
|---|---|
| 뼈대 | **항상 있는 값.** 제목 · 갈래 이름 · 메뉴 |
| 살 | 껐다 켤 수 있는 값. 메일 · 실명 · 사진 · 계정 |

**04 산출물에서 「켜고 끄기」가 붙은 요소를 먼저 추려낸다.**
그중 하나라도 제목 자리에 있으면 **04 를 안 고치고 05 에서 제목 자리를 따로 뗀다.**
그리고 `지은것.md` 의 「04 로 넘길 것」에 적는다.

## ⚑ ★ 색은 근거 문서에서만 가져온다

**04 화면설계서 파일에는 색이 두 종류로 섞여 있다.**

| | |
|---|---|
| **미리보기 안의 색** | **사이트 색이다.** 진짜 색으로 그려 놨다 |
| **문서를 꾸미는 색** | **사이트 색이 아니다.** 화면설계서라는 문서 자체의 스타일이다 |

**파일을 통째로 읽고 색을 뽑으면 둘이 섞인다.**

> **색 · 글꼴 · 간격은 `근거.html` 에서만 가져온다.**
> **화면설계서는 짜임새와 요소를 보는 데 쓴다.**

**근거에만 있고 미리보기에 안 나온 색이 있다.** 상태 색과 경고 색이 대개 그렇다.
**그것도 써야 한다.** 미리보기에 안 보인다고 빼지 않는다.

## ⚑ 04 가 정한 것을 다시 정하지 않는다

**04 가 표시 종류까지 정해 놨다.** 05 는 **붙이는 쪽**이다.

| | 누가 |
|---|---|
| 어느 화면에 무슨 표시가 붙나 | **04** |
| 그 값이 어느 칸에서 오나 | **04** |
| **실제로 붙이고 값을 채우기** | **05** |

**04 산출물에 표시가 안 적힌 화면이 있으면** 04 로 돌려보내지 말고
**05 가 정하고 `지은것.md` 에 「04 에 없어서 05 가 정했다」고 적는다.**

## ⚑ 개수를 미리 박지 않는다

**화면 몇 장 · 파일 몇 개 · 며칠 걸린다를 약속하지 않는다.**
04 산출물이 사람마다 다르고, 짓는 시간은 더 다르다.

**조건을 건다.**

| | 조건 |
|---|---|
| 1 | **04 화면 종류가 전부 실제로 열린다** |
| 2 | **받아본 결과에 ✕ 가 0개다.** 못 고친 것은 왜 못 고치는지 적혀 있다 |
| 3 | **관리 화면이 셋 다 막혀 있다** (표시 · 로봇 · 사이트맵) |
| 4 | **틀마다 실물 하나로 열어봤다** |

**클릭 다섯 개만 개수를 박는다.**

## ⚑ ★ 「이 틀로 실물 하나를 만들어봤나」

**04 는 「글 한 편」 화면을 한 장으로 그렸고, 그 틀을 글 여러 편이 쓴다.**
그런데 **지금 글이 0편이면 넣어볼 게 없다.**

| | |
|---|---|
| 그냥 넘어가면 | **틀이 맞는지 틀린지 모르는 채로 통과한다** |
| 그러면 | **06 이 첫 글을 쓸 때 그제야 터진다** |

> **틀마다 실물 하나를 만들어 화면을 열어본다. 하나면 된다.**
> **개수를 박는 게 아니라 「돌아가나」를 보는 것이다.**

**임시로 만든 것은 지운다.** 지우고 나서 **지웠다고 적는다.**

## ⚑ 어려운 말을 그대로 안 쓴다

**05 는 처음으로 진짜 코드를 짓는 단계라 어려운 말이 쏟아진다.**

이 문서 안에서는 그대로 쓴다. **파일 안도 그대로 둔다.**
**바꾸는 건 화면에 내는 말과 산출물의 설명뿐이다.**

| 우리 말 | 화면에 낼 말 |
|---|---|
| 빌드 | **짓기** |
| 배포 | **인터넷에 올리기** |
| 리포지토리 | **코드를 두는 곳** |
| 커밋 · 푸시 | **저장하기 · 올리기** |
| 의존성 · 패키지 | **같이 깔리는 것** |
| 서버 렌더링 | **미리 만들어서 보내기** |
| 라우팅 | **주소마다 어느 화면을 보여줄지** |
| 컴포넌트 | **되풀이 쓰는 조각** |
| 환경변수 | **밖에 안 보이게 두는 설정값** |
| 스키마 · JSON-LD | **기계가 읽는 표시** |
| `robots.txt` | **어느 로봇이 들어와도 되는지 적는 파일** |
| 사이트맵 | **글 목록을 기계에게 알려주는 파일** |
| 캐노니컬 | **대표 주소** |
| `alt` | **그림이 안 보일 때 대신 읽히는 글** |
| 헤더 · 응답 머리글 | **사이트가 같이 보내는 안내표** |
| 프로덕션 · 프리뷰 | **올린 것 · 미리보기** |
| 도메인 | **사이트 주소** |

### 처음 한 번은 풀어서 같이 적는다

그 말이 처음 나오는 자리에서만 괄호로 원래 말을 붙인다. **두 번째부터는 안 붙인다.**

## ⚑ 첫 화면에 내면 안 되는 말

**"04 산출물 {N}자를 읽었고 화면이 {N}종류입니다" 같은 말을 첫 화면에 내지 않는다.**
우리한테 필요한 말이지 쓰는 사람한테는 겁만 준다.

설정 읽기, 04·03·02 산출물 읽기, 요소 세기는 **조용히** 한다.
**짓는 동안 진행 상황을 쏟지 않는다.** 파일 이름을 하나씩 읽어주지 않는다.

## ⚑ 출력이 터지지 않게 한다

01 이 화면 출력에서 막혔고 03 은 위키 목차, 04 는 화면 목록이 그 자리였다.
**05 는 파일 목록과 오류 메시지가 그 자리다.**

| | |
|---|---|
| 파일 목록 | **화면에 안 낸다.** `지은것.md` 에 있다 |
| 짓는 중 나오는 글 | **화면에 안 쏟는다.** 끝나고 결과만 |
| 오류 | **무슨 오류인지 한 줄로 풀어서.** 원문을 통째로 붙이지 않는다 |
| 검사 결과 | **✕ 만 낸다.** ○ 는 개수로 |

## ⚑ 예시를 지어낸다

앞사람이 만든 사이트가 잘 나와 있으면 그걸 본보기로 삼고 싶어진다.
**그 안에 든 건 전부 한 사람의 값이다.**

| 박히면 안 되는 것 | |
|---|---|
| 주소 · 도메인 | 누구의 것이든 |
| 색 코드 · 글꼴 이름 | 그 사람이 고른 것 |
| 화면 문장 · 메뉴 이름 | 그 사람이 쓴 것 |
| 갈래 이름 | 그 사람 주제에서 나온 것 |
| 앞 회차 숫자 | 화면 몇 종류 · 요소 몇 개 · 글 몇 편 |

> **규칙. 이 문서에 쓰는 예시는 전부 지어낸 것이다.
> 그리고 「지어낸 예시입니다」라고 적는다.**

**고칠 때마다 `grep` 을 돌리는 것 말고는 되는 방법이 없다.**

## ⚑ 오탈자

보내기 전에 한 번 읽는다. 한 글자 틀리면 "대충 만들었네"로 읽히고 그 뒤 신뢰가 통째로 떨어진다.
**아래 고정 문구는 그대로 복사해서 쓴다.** 즉흥으로 다시 쓰지 않는다.

---

## 이 스킬이 진짜 하는 일

04 가 만든 건 **그림과 설명**이다. 요소마다 다섯 가지가 적혀 있다.
**그런데 그건 아직 아무도 못 본다.**

```
04 가 준 것        화면 그림 · 요소마다 다섯 가지 · 색과 글꼴 값
   ↓
파일로만 있어서 이 컴퓨터 밖에서는 아무도 못 본다
   ↓
그걸 실제로 짓고 인터넷에 올린다
   ↓
그리고 ★ 받아봐서 진짜 되는지 확인한다              ← 여기가 05 의 심장
   ↓
06 이 그 위에 첫 글을 낸다
```

### 왜 「받아보기」가 심장인가

**앞 넷은 만든 것이 곧 결과였다. 05 는 아니다.**

| | |
|---|---|
| 01~04 | **파일을 만들면 끝.** 열어보면 맞는지 보인다 |
| **05** | **파일을 만들고, 올리고, 그게 밖에서 어떻게 보이는지가 결과다** |

**중간에 조용히 막히는 자리가 여럿이다.** 올린 곳의 설정 하나가 다 뒤집는다.
**그래서 짐작하지 않고 받아본다.**

---

## 산출물

**사이트 자체가 제일 큰 산출물이다.** 문서는 그걸 설명하는 것이다.

```
{작업폴더}/site/           ★ 사이트. 여기서 짓는다
{작업폴더}/outputs/05-build/
├── plan.md               ★ 여기부터 본다
├── 검사표.md              ★ 받아본 결과. 05 가 채워서 낸다
└── 지은것.md              무엇으로 어디에 · 깐 것 · 지우는 법
```

### 검사표는 05 가 채워서 낸다

| | |
|---|---|
| 빈 칸으로 내면 | **손일을 사람에게 넘기는 것이다** |
| 05 가 채우면 | 사람은 **읽고 짚기만** 한다 |

### ★ 다만 이건 자기 채점이다. 그래서 칸이 셋이다

**05 가 지은 걸 05 가 잰다.** 헐겁게 재도 05 안에서는 아무도 못 잡는다.

> **그래서 검사표를 세 번 쌓는 문서로 만든다.**

```
| 항목 | 05 가 잰 것 | 06 이 잰 것 | 07 이 잰 것 |
```

| 누가 | 언제 | 무슨 자로 |
|---|---|---|
| **05** | 짓고 올린 직후 | `받아보기.sh` |
| **06** | **글을 쓰기 전에 한 번** | **같은 `받아보기.sh`.** 다른 스킬이 같은 자로 |
| **07** | 감사할 때 | **감사 도구.** 다른 자로 |

**둘이 잡는 게 다르다.**

| | 무엇을 잡나 |
|---|---|
| 같은 자 · 다른 스킬 | **05 가 잘못 적었거나, 올린 뒤에 바뀐 것** |
| 다른 자 | **우리 자에 없는 항목** |

**검사표 맨 위에 이 줄을 반드시 적는다.**

> 이건 05 가 자기가 지은 걸 자기가 잰 것입니다. 06 과 07 이 다시 잽니다.

**04 가 이미 같은 방식이었다.** 03 산출물이 다 자리를 가졌나를 04 가 쟀다.
**다음 단계가 앞 단계를 재는 것이 이 체인의 방식이다.**

### `지은것.md` 에 지우는 법을 반드시 적는다

**깐 것이 있는데 지우는 법이 없으면 그 문서는 미완성이다.**
쓰는 사람은 나중에 「이게 뭐였지」 하고 못 지운다.

### 안 만드는 것

| | 어디서 |
|---|---|
| 글 본문 | **06 PUBLISH** |
| 첫 글 발행 · 말투 다듬기 · 게이트 검사 | **06 PUBLISH** |
| 감사 도구를 돌려 재기 · 반증 걸러내기 | **07 TECH.** 05 는 받아보기까지 |
| 지표 · 검색 콘솔 | **08 MEASURE** |
| 화면을 더하거나 요소를 바꾸기 | **04 DESIGN.** 05 는 04 산출물을 안 고친다 |

---

## 절대 규칙

| | |
|---|---|
| **받아보기** | **파일만 보고 통과시키지 않는다.** 올린 주소를 받아본다 |
| **자기 채점** | **05 가 잰 건 자기 채점이다.** 검사표에 그렇게 적고 06·07 이 다시 잴 칸을 비워 둔다 |
| **짐작 금지** | **받아본 것만 검사표에 적는다.** 못 쟀으면 못 쟀다고 적는다 |
| **색** | **`근거.html` 에서만 가져온다.** 화면설계서 파일의 문서 색을 안 옮긴다 |
| **없는 복잡함** | **`API` 가 「없음」이면 그 자리를 안 만든다** |
| **04 산출물** | **안 고친다.** 화면을 더하거나 요소를 바꾸지 않는다 |
| **깐 것** | **무엇을 왜 깔았고 어떻게 지우는지 적는다.** 「아무것도 안 깝니다」를 안 쓴다 |
| **관리 화면** | **표시 · 로봇 · 사이트맵 셋을 다 막는다.** 하나만 빠져도 검색에 뜬다 |
| **개인정보** | **연락처와 계정 주소가 코드에 박힌다.** 넣기 전에 한 번 짚는다 |
| **열쇠** | **밖에 보이는 자리에 안 둔다.** 실수로 올라가면 되돌리기 어렵다 |
| **굳히기** | **굳혀두는 층을 세고 짓는다.** 글이 자주 바뀌면 안 굳히는 쪽으로 시작한다 |
| **조용한 실패** | **떨어질 때 소리를 낸다.** `catch {}` 로 넘어가지 않는다 |
| **제목 자리** | **껐다 켤 수 있는 값을 제목으로 안 쓴다.** 껐을 때 화면이 무너진다 |
| **문구** | **구조를 바꾸면 그 구조를 설명하는 화면 문구도 같이 고친다** |
| **주소** | **한 번 정하면 안 바꾼다.** 바꾸면 그동안 걸린 링크가 죽는다 |
| **공짜 주소** | **「검색에 안 나온다」고 말하지 않는다.** 사실이 아니다. `references` 를 본다 |
| 예시 | **앞사람 산출물에서 안 가져온다. 지어내고 지어냈다고 적는다** |
| 절대경로 | 산출물에 안 적는다. 사용자 이름이 박혀 있다 |
| 개수 | **화면 장수 · 파일 개수 · 걸리는 시간을 미리 안 박는다** |
| 클릭 | **다섯 개.** 실제 개수를 화면에 쓴다. 이것만 개수를 박는다 |
| 06·07 의 일 | **미리 하지 않는다.** 글 · 발행 · 감사 |
| 자동 커밋 | **스킬이 알아서 `git commit` 하지 않는다** |
| 어려운 말 | 안 쓴다. 써야 하면 그 자리에서 한 줄로 푼다 |
| 중간에 멈춤 | **앞 화면까지만 돼도 사이트가 선다.** 완주를 조건으로 걸지 않는다 |
| 내부 문장 | 이 문서의 규칙을 화면에 읽어주지 않는다 |
| 대시와 불릿 | 화면에 내는 문구에 대시(—)와 하이픈 불릿을 쓰지 않는다. 표나 번호로 쓴다 |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
| 시간 | **숫자로 약속하지 않는다** |

---

## 대화 원칙

**상담이 아니라 작업이다.** 그런데 결과를 볼 때는 **재미있어야** 한다.

| ✕ 깎기 | ✕ 치켜세우기 |
|---|---|
| "이 구조로는 좀 어렵겠는데요" | "사이트가 정말 잘 나왔네요" |

둘 다 평가다. 대신 **형용사를 빼고 사실과 발견만** 내놓는다.

```
✕  "완벽하게 배포됐습니다!"
○  "올렸고 받아봤어요. 열 항목 중 여덟이 ○ 고 둘이 ✕ 입니다. 둘 다 고칠 수 있어요."
```

**★ 05 는 「됐다」고 말하고 싶어지는 자리다.** 사이트가 눈에 보이니까.
**받아보기 전에는 됐다고 말하지 않는다.**

**04 를 막 끝낸 사람이다.** 04·03·02 에서 이미 답한 것을 다시 묻지 않는다.
화면과 요소는 `화면설계서.html` 에, 색과 글꼴은 `근거.html` 에,
주소 후보는 `구조.md` 에, 이름과 계정은 `컨셉.md` 에 있다. **읽고 쓴다.**

---

## 진행 표시

클릭 질문마다 앞에 `[2/5]` 를 붙인다. **그게 전부다.**

**짓는 동안에는 진행률을 안 낸다.** 대신 **단계가 바뀔 때 한 줄**을 낸다.

```
앞 화면부터 짓겠습니다. 조금 걸립니다.
```

### 클릭 질문은 반드시 하나씩

| | |
|---|---|
| 1 | 질문 하나면 탭이 없고 고르는 즉시 넘어간다 |
| 2 | 보기는 2~4개, 각각 한 줄 설명 |
| 3 | **빠져나갈 보기를 하나 둔다.** "잘 모르겠다" · "알아서 정해줘" |
| 4 | 후보 비교가 필요하면 preview 를 쓴다. **주소 후보가 그 자리다** |

### 서술로 받는 것

넷뿐이다. **클릭 개수에 안 센다.**

| | |
|---|---|
| 1 | 올릴 곳 계정을 만들고 오셨으면 (클릭 ② 뒤) |
| 2 | 담을 데 계정을 만들고 오셨으면 (클릭 ③ 뒤) |
| 3 | 주소를 사고 오셨으면 그 주소 (클릭 ⑤ 뒤) |
| 4 | "고칠 데" (`plan.md` 뒤) |

**클릭 다섯 개가 전부 「고르기」이지 「하기」가 아니다.**

### 손일을 사람에게 넘기지 않는다

**05 에는 사람만 할 수 있는 일이 셋 있다.**

| | 왜 사람만 하나 |
|---|---|
| **올릴 곳에 계정 만들기** | 스킬이 대신 가입 못 한다 |
| **담을 데에 계정 만들기** | 같은 이유 |
| **주소 사기** | 결제가 필요하다 |

> **셋 다 「사용자가 직접 하세요」로 끝내지 않는다. 클릭 순서를 적어준다.**
> **그리고 셋 다 건너뛸 수 있게 한다.** 안 해도 05 는 끝난다.

**계정을 만들고 오시면 그다음은 05 가 한다.**
담을 자리를 만드는 것 · 설정값을 넣는 것 · 시험해 보는 것은 전부 05 가 한다.
1회차에서 사용자가 클릭으로 만들려다 안 만들어진 채 넘어간 적이 있다.
**05 가 직접 만들고, 만들어졌는지 세어서 확인한다.**

**나머지는 전부 05 가 한다.** 파일을 만들라고 시키지 않는다.

---

## STEP 1 · 첫 메시지

시작하자마자 여섯 가지를 말한다.
에셋엔진이 뭔지, 이 단계가 그중 어디인지, 뭘 하는 건지, **여기서 안 하는 게 뭔지**,
어떻게 가는지, **미리 알아둘 게 뭔지**.

**아래를 마크다운 그대로 낸다.** 코드블록으로 감싸지 않는다.
선이나 네모로 모양내지 않는다.

**낸 뒤 멈춘다.** 곧바로 질문 창을 띄우지 않는다.

내는 내용은 이것이다.

---

## 05 BUILD · 사이트 짓기

### 에셋엔진이 뭐냐면

한 일을 **꺼내고, 성과의 언어로 옮기고, 하나의 구조로 이어**
검색과 AI 답변에서 발견되게 만드는 워크플로우입니다.

단계마다 스킬 하나씩이고, 끝나면 다음 스킬을 알려드립니다.
**한 번에 하나만 아시면 됩니다.**

| | |
|---|---|
| 01 재료화 | 흩어진 자료를 한 형식으로 모았습니다 |
| 02 컨셉 잡기 | 컨셉 한 줄을 정하고 이름을 맞췄습니다 |
| 03 구조 잡기 | 주제를 묶고 글 목록을 뽑았습니다 |
| 04 화면설계 | 화면을 그리고 색과 글꼴을 정했습니다 |
| **05 사이트 짓기** | **지금 여기** |
| 06 첫 글 내기 | 방금 만든 관리 화면에서 첫 글을 냅니다 |
| 07 검색·AI 점검 | 사이트가 선 뒤 실제로 재고 고칩니다 |
| 08 지표 | 효과를 재고, 다시 06 으로 돌아갑니다 |

### 이 단계가 뭐냐면

04 에서 **화면을 그리셨습니다.** 요소마다 만드는 순서까지 적혀 있고요.

**그런데 그건 아직 파일입니다. 이 컴퓨터 밖에서는 아무도 못 봅니다.**

**여기서는 그걸 실제로 짓고 인터넷에 올립니다.**
그리고 **올린 주소를 제가 받아봐서 진짜 되는지 확인합니다.**

### 왜 받아보는 게 중요하냐면

**파일에는 다 맞게 적혀 있는데 올리고 나면 막혀 있는 일이 흔합니다.**

올린 곳의 설정 하나가 로봇을 막고 있거나, 미리보기 주소를 올린 주소로 착각하거나요.
**눈으로는 멀쩡해 보입니다.** 그래서 받아봐야 압니다.

### 여기서 안 하는 게 있습니다

| | |
|---|---|
| **글을 쓰지 않습니다** | 06 에서 씁니다. 여기서는 **글이 들어갈 자리**까지 |
| **첫 글을 내지 않습니다** | 06 에서 냅니다 |
| **검색 점검 도구를 안 돌립니다** | 07 에서 합니다. 여기서는 **제가 직접 받아봅니다** |
| **지표를 안 잽니다** | 08 에서 합니다 |
| **화면을 다시 안 그립니다** | 04 에서 그린 것을 그대로 짓습니다 |

### 어떻게 가냐면

질문 다섯 개면 끝납니다. 그사이에 제가 짓는 시간이 좀 걸립니다.

| | | |
|---|---|---|
| 1 | 04 에서 그린 화면을 읽습니다 | 기다리시면 됩니다 |
| 2 | **주소를 어떻게 만들지** 고르기 | 질문 1개 |
| 3 | **어디에 올릴지** 고르기 | 질문 1개 |
| 4 | **글과 사진을 어디에 담을지** 고르기 | 질문 1개 |
| 5 | **앞 화면을 짓고 올립니다** | 기다리시면 됩니다 |
| 6 | **받아봐서 잰 결과를 보여드림** | |
| 7 | **✕ 를 고칠지** 고르기 | 질문 1개 |
| 8 | **사이트 주소를 지금 살지** 고르기 | 질문 1개 |
| 9 | **관리 화면을 붙이고 다시 올립니다** | 기다리시면 됩니다 |

중간에 멈추셔도 되고, 모르는 건 안 정하고 넘어가셔도 됩니다.

### 끝나면 뭘 받냐면

**사이트 하나와 문서 3장**입니다.

| | |
|---|---|
| **사이트** | **인터넷에 올라가 있습니다.** 주소를 알려드립니다 |
| `검사표.md` | **제가 받아봐서 잰 결과.** 항목마다 ○·✕ 와 고친 것 |
| `지은것.md` | 무엇으로 어디에 지었나 · **깐 것과 지우는 법** |
| `plan.md` | 지금 어디까지 됐고 다음에 뭘 하나 |

### 미리 알려드릴 것

| | |
|---|---|
| **깝니다** | **여기서는 뭔가를 깔아야 합니다.** 앞 단계들과 다릅니다. **무엇을 왜 깔고 어떻게 지우는지 문서에 적어 둡니다** |
| **순서** | **앞 화면을 먼저 세워서 올리고, 그다음 관리 화면을 붙입니다.** 중간에 멈춰도 사이트는 서 있습니다 |
| **받아보기** | **다 짓고 나서 제가 그 주소를 받아봅니다.** 눈으로 멀쩡해 보여도 막혀 있을 수 있어서요 |
| **글** | 지금은 글이 0편입니다. **글이 들어갈 자리**까지 짓습니다. 글은 06 에서 씁니다 |
| **담을 데** | **글과 사진을 담아둘 데를 여기서 정합니다.** 이미 쓰시는 게 있으면 그걸 씁니다. 없으면 무료로 되는 곳으로 계정 만드는 순서를 알려드립니다. **여기서 안 정하면 나중에 지은 걸 다시 지어야 합니다** |
| **만든 것** | 04 에서 캐두신 것은 **지금 채웁니다.** 그건 내용이 이미 있어서요 |
| **관리 화면** | **동작까지 만듭니다.** 그리고 **검색에 안 뜨게 막습니다** |
| **사이트 주소** | 02 에서 이름을 정하셨죠. **사는 건 여기서 하셔도 되고 안 하셔도 됩니다.** 안 사도 사이트는 서고 검색에도 나옵니다 |
| **연락처** | 04 에서 연락 칸을 두셨으면 **그 값이 코드에 들어갑니다.** 넣기 전에 한 번 여쭤봅니다 |
| **개수** | 몇 장을 짓겠다고 미리 약속하지 않습니다. 다 짓고 나서 알려드립니다 |

---

**지금 다 이해 안 되셔도 됩니다.** 끝까지만 오시면 사이트가 서고,
문서에 뭐가 어떻게 됐는지 다 적혀 있습니다.

다 읽으셨으면 **아무 말이나 보내주세요.** 바로 시작할게요.
궁금한 게 있으면 먼저 물어보셔도 됩니다.

---

**이 안내는 한 번만 낸다.** 사용자가 답하면 바로 STEP 2 로 간다.

---

## STEP 2 · 04·03·02 산출물 읽기

**화면에 한 줄만 낸다.** 읽은 내용을 보고하지 않는다.

### 설정을 조용히 읽는다

```bash
cat ~/.claude/iny-config.json
```

`kb_path` 를 꺼내 작업 폴더로 쓴다. `~` 는 실제 홈 경로로 바꾼다.
파일이 없으면 **작업 폴더가 어디인지 묻는다.** 짐작해서 뒤지지 않는다.

### 읽는 것

```
{작업폴더}/outputs/04-design/화면설계서.html      ★ 통째로
{작업폴더}/outputs/04-design/근거.html            ★ 색과 글꼴은 여기서만
{작업폴더}/outputs/04-design/plan.md
{작업폴더}/outputs/03-structure/구조.md
{작업폴더}/outputs/02-identify/컨셉.md
{작업폴더}/wiki/                                  글 틀의 뼈대
```

### ★ 먼저 세어본다. 화면에는 안 낸다

**짓기 전에 이 셋을 세면 무엇을 안 만들어도 되는지 안다.**

| 세는 것 | 알게 되는 것 |
|---|---|
| `API` 가 「없음」인 개수 | **바깥 서비스를 다룰 자리가 있나** |
| `Logic` 이 「없음」인 개수 | **동작을 만들 자리가 몇 개인가** |
| 화면 종류 개수 | 얼마나 지어야 하나 |

**「없음」이 대부분이면 그 층을 아예 안 만든다.**

### 무엇을 어디에 쓰는지

| 앞 단계가 준 것 | 05 가 쓰는 자리 |
|---|---|
| 04 **화면 종류와 요소** | **짓는 지시서.** 요소마다 다섯 층 그대로 |
| 04 **표시 종류와 어느 칸에서 오나** | ★ **그대로 붙인다.** 05 가 다시 정하지 않는다 |
| 04 **화면 옆 주소** | 04 의 잠정안. **05 가 클릭 ① 로 확정한다** |
| 04 **상태 다섯** | 누를 수 있는 것마다 |
| 04 **「폰에서 볼 때」** | 화면이 좁을 때 |
| 04 **글이 0편일 때 화면** | **그대로 짓는다.** 지금이 그 상태다 |
| 04 **어드민 뼈대** | STEP 10 |
| 04 **만든 것 목록** | ★ **내용이 이미 있다. 지금 채운다** |
| `근거.html` **색 · 글꼴 · 간격 · 깊이 · 움직임** | ★ **여기서만 가져온다** |
| `근거.html` **「안 하는 것」** | 05 도 그대로 안 한다 |
| 03 **주소 후보 둘 · 갈래 이름 후보** | 클릭 ① |
| 03 **`wiki/`** | 글 틀의 뼈대. **글이 아니다** |
| 02 **표기 네 칸** | 사이트 이름 · 저자 이름 |
| 02 **정한 사이트 주소 이름** | ★ **클릭 ⑤ 에서 쓴다** |
| 02 **계정 주소** | 계정을 묶는 표시의 값 |
| 02 **컨셉 한 줄 · 소개 한 줄** | 첫 화면 문장 · 두 줄 요약 |

### 없으면 여기서 멈춘다

```
04 에서 만든 화면설계서가 안 보입니다.

05 는 04 가 그린 화면을 실제로 짓는 단계라
04 를 먼저 하셔야 합니다.

  https://github.com/iny101400-young/iny-design

받으시려면 이 폴더에서 아래를 그대로 치시면 됩니다.

  git clone https://github.com/iny101400-young/iny-design.git
  cd iny-design
  bash install.sh

깔고 나서 Claude Code 를 다시 열고 "화면 그리기" 라고 치세요.
```

`화면설계서.html` 은 있는데 `근거.html` 이 없으면 **멈추지 않고 간다.**
다만 **색을 화면설계서 미리보기에서 뽑고, 그렇게 했다고 `지은것.md` 에 적는다.**

### 화면에는 이 한 줄만

```
04 에서 그리신 화면을 읽었어요.

  화면 {N}종류 · 요소 {N}개

이제 이걸 실제로 짓겠습니다.
```

---

## STEP 3 · 주소를 어떻게 만들까

**03 이 후보 둘을 냈고 04 는 안 건드렸다. 05 가 고른다.**

**주소는 나중에 바꾸면 그동안 걸린 링크가 다 죽는다.** 짓기 전에 정한다.

### 화면에는 이렇게 나간다

```
주소부터 정합니다. 나중에 바꾸면 그동안 걸린 링크가 다 죽어서
짓기 전에 정하는 게 낫습니다.

03 에서 후보 둘을 내주셨어요. 여기에 「주소를 한글로 둘지」가 겹칩니다.
```

**[1/5] 주소를 어떻게 만들까요?** (단일 선택 · preview 를 쓴다)

| 보기 | 설명 |
|---|---|
| `짧게 · 영문` | `갈래를 주소에 안 넣습니다. 제일 짧고 안 깨집니다` |
| `짧게 · 한글` | `갈래를 안 넣고 글 이름만 한글로. 사람이 읽기 좋습니다` |
| `갈래까지 · 영문` | `주소만 봐도 무슨 갈래 글인지 압니다. 길어집니다` |
| `알아서 정해줘` | `짧게 · 영문으로 갑니다` |

**preview 에 실제로 어떻게 보이는지 그려서 낸다.**

### 한글 주소의 값과 대가를 같이 말한다

| | |
|---|---|
| 값 | **한국어로 검색하는 사람에게 주소가 그대로 읽힌다** |
| 대가 | **복사해 붙이면 알아보기 힘든 글자로 늘어난다** |
| 안 갈리는 것 | 검색이 한글 주소를 못 읽지는 않는다 |

### 묻지 않고 정하는 것

[`references/짓는규칙.md`](references/짓는규칙.md) 1장에 있다.

| | |
|---|---|
| 소문자로 고정 | 02 표기와 같은 이유다 |
| 날짜를 안 넣는다 | 글이 낡아 보이고 고치면 주소가 죽는다 |
| 낱말은 하이픈으로 잇는다 | |
| 주소 끝 슬래시를 **한쪽으로 고정** | 둘이 다른 주소가 된다 |
| **모든 공개 화면이 대표 주소를 갖는다** | 없으면 검색이 스스로 고른다 |

---

## STEP 4 · 어디에 올릴까

### 무엇으로 지을지는 안 묻는다

**05 가 정한다.** 개발을 모르는 사람에게 물으면 고를 근거가 없다.

| 무엇을 보고 정하나 | |
|---|---|
| 1 | **스펙과 `CLAUDE.md` 에 적힌 조건.** 「미리 만들어서 보내기」가 대개 그것이다 |
| 2 | 04 `API` 가 「없음」인 개수. **바깥 서비스가 없으면 가벼운 쪽** |
| 3 | 글을 파일로 두는가. **그러면 글 파일을 그대로 읽는 방식** |

**고른 이유를 `지은것.md` 에 적는다.** 화면에는 「이걸로 짓겠습니다」 한 줄만.

### 올릴 곳은 묻는다

**계정이 필요하고 사람만 만들 수 있어서다.**

```
지은 걸 인터넷에 올려야 다른 사람이 볼 수 있어요.
올리는 곳은 계정이 필요해서 여쭤봅니다.
```

**[2/5] 어디에 올릴까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `이미 쓰는 데가 있다` | `어디인지 말씀해 주세요. 거기 맞춰서 짓습니다` |
| `없다. 무료로 되는 곳으로` | `계정 만드는 순서부터 알려드립니다. 카드 없이 됩니다` |
| `아직 안 올리고 내 컴퓨터에서만 볼래` | `짓기만 하고 안 올립니다. 나중에 올리실 수 있습니다` |
| `알아서 정해줘` | `무료로 되는 곳으로 갑니다` |

### 계정 만들기를 클릭 순서로 적어준다

**「가입하세요」로 끝내지 않는다.**

**서비스마다 화면이 다르고 이름도 바뀐다.** 「대체로 이런 자리에 있습니다」로 적는다.
**모르는 것은 모른다고 적는다.**

### 「안 올린다」를 고르면

**멈추지 않는다.** 짓고, 컴퓨터에서 열어보고, 검사 중 **받아봐야 아는 것은 「안 잼」**으로 적는다.

> **「나중에」라고 적지 않는다. 「올리면 잴 수 있습니다」라고 적는다.**

### ★ 글과 사진을 어디에 담을까 · 짓기 전에 정한다

**미루면 지은 걸 다시 짓는다.**

1회차에서 05 는 이걸 「사진을 켤 때 정하겠습니다」로 미뤘다. 그 값을 06 을
시작하려는 자리에서 치렀다. 담을 데를 뒤늦게 붙이느라 화면 41군데를 다시 고치고,
07 감사를 처음부터 다시 돌리고, 배포를 다섯 번 다시 했다.

**「지금 안 정해도 되는 것」이 아니라 「지금 정해야 다시 안 짓는 것」이다.**

먼저 묻는다. **이미 쓰는 게 있으면 그걸 쓴다.**

```
글하고 사진을 담아둘 데가 필요합니다.
이미 쓰시는 게 있으면 그걸 쓰는 게 낫습니다. 익숙한 게 성능보다 셉니다.
```

**[3/5] 글과 사진을 어디에 담을까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `이미 쓰는 데가 있다` | `어디인지 말씀해 주세요. 거기 맞춰서 짓습니다` |
| `없다. 골라줘` | `Supabase 로 갑니다. 계정 만드는 순서부터 알려드립니다` |
| `사이트 파일에 같이 둔다` | `계정이 안 늘어납니다. 대신 글을 고칠 때마다 다시 올려야 하고, 관리 화면은 이 컴퓨터에서만 저장됩니다` |

### 「골라줘」면 Supabase 하나만 댄다

**셋을 늘어놓지 않는다.** 개발을 모르는 사람은 못 고른다.

**무료 한도와 함정을 같이 적는다.** 「무료입니다」로만 적으면 나중에 막힌다.

| | |
|---|---|
| 파일 보관 | 1 GB |
| 데이터베이스 | 500 MB |
| 한 달 전송량 | 5 GB |
| **함정** | **1주일 아무도 안 오면 잠든다.** 잠들면 사진이 안 뜨고 저장이 안 된다 |

**잠들지 않게 하루 한 번 두드리는 자리를 같이 짓는다.** 사람이 기억할 일로 남기지 않는다.

### 특정 회사에 묶이는 선택지에는 그렇다고 적는다

Vercel 에 올리는 사람이면 Vercel Blob 도 된다. **한 줄로만 적는다.**
이건 Vercel 에 올린 사람만 쓸 수 있다. 그 사실을 안 적으면 받아 쓴 사람이 막힌다.

### 정했으면 짓기 전에 이 셋을 확인한다

| | 무엇 | 왜 |
|---|---|---|
| 1 | **파일 이름을 영문으로 새로 지어서 올리나** | 한글 이름을 안 받는 곳이 있다. 사람이 고른 파일 이름을 그대로 쓰면 막힌다 |
| 2 | **읽을 때 주소를 매번 다르게 하나** | 담을 데가 지역마다 응답을 따로 갖고 있다. 「새것 달라」는 머리글만으로는 안 통했다 |
| 3 | **담을 데가 잠들었을 때 무엇으로 떨어지나** | 떨어질 데가 있어야 사이트가 같이 안 선다. 그리고 **떨어질 때 반드시 소리를 낸다** |

---

## STEP 5 · 앞 화면을 짓는다

**화면에 한 줄만 낸다.**

```
앞 화면부터 짓겠습니다. 조금 걸립니다.
```

**짓는 곳은 `{작업폴더}/site/` 다.** 이미 있으면 **덮어쓰지 않고 묻는다.**

### 무엇부터 짓나

[`references/짓는규칙.md`](references/짓는규칙.md) 2장을 읽고 따른다.
**담을 데를 붙이는 것은 같은 문서 5장이다.**

| | |
|---|---|
| 1 | **되풀이 쓰는 값을 먼저 박는다.** 색 · 글꼴 · 간격 · 모서리 |
| 2 | **담을 데를 붙인다.** 클릭 ③ 에서 정한 곳 (5장) |
| 3 | **모든 화면에 들어가는 머리와 발** |
| 4 | **내용이 이미 있는 화면부터** |
| 5 | **글이 0편일 때 화면** |
| 6 | **없는 주소 화면** |

**담을 데를 2번에 두는 이유가 있다.** 화면을 다 짓고 나서 붙이면
글을 읽어오는 자리를 전부 다시 고쳐야 한다. **1회차에 41군데를 다시 고쳤다.**

**내용이 이미 있는 화면부터 짓는 이유가 있다.** 그래야 **일찍 올려서 일찍 잴 수 있다.**

### 04 가 캐둔 것을 지금 채운다

**04 가 「만든 것」을 자료에서 캐서 표로 채워 놨다.** 그건 **글과 달리 지금 내용이 있다.**

| | |
|---|---|
| 채운다 | 이름 · 무엇 · 언제 · 지금 볼 수 있나 |
| **「재료에 없음」이라고 적힌 칸** | **비운다.** 지어내지 않는다 |
| 그림 | 04 가 자리만 잡았으면 **자리만 짓는다** |

**안 채우면 빈 사이트가 된다.** 글이 0편인데 만든 것까지 비면 볼 게 없다.

### 표시를 붙인다

**04 가 종류와 「어느 칸에서 오나」를 정해 놨다. 그대로 붙인다.**

| | |
|---|---|
| **본문에 글자로 안 보이는 값을 안 넣는다** | 화면에 그 글자가 없으면 넣지 않는다 |
| **계정을 묶는 자리는 한 번만** | 여러 곳에 적으면 고칠 때 어긋난다 |
| **관리 화면에는 안 붙인다** | STEP 10 |
| 04 에 안 적힌 화면이 있으면 | **05 가 정하고 「04 에 없어서 정했다」고 적는다** |

### 로봇 파일 · 사이트맵 · `llms.txt`

[`references/짓는규칙.md`](references/짓는규칙.md) 3장에 있다.

| | |
|---|---|
| **로봇** | **막지 않는다.** 막아도 인용은 그대로 일어나고 방문만 잃는다 |
| **로봇 이름 목록** | **이 파일에 안 박아 둔다.** 계속 바뀐다. **지을 때 확인한다** |
| **사이트맵** | **공개 주소만.** 관리 화면과 아직 없는 글은 안 넣는다 |
| **`llms.txt`** | 넣는다. **다만 근거가 없다고 `지은것.md` 에 적는다** |
| 시각 | **손으로 안 적는다.** 파일에서 자동으로 온다 |

### 개인정보가 들어가는 자리

**04 가 연락 칸을 그려 뒀으면 그 값이 코드에 들어간다.**

> **넣기 전에 한 번 짚는다. 넣고 나서 지우면 이미 늦다.**

```
연락처를 넣으면 올린 순간 그대로 공개됩니다.
04 에서 관리 화면에 끄는 자리를 같이 그려 두셨어요.

지금 넣을까요, 비워 두고 나중에 관리 화면에서 켤까요?
```

**서술로 받는다. 클릭에 안 센다.**

**02 가 「실명이 든 주소」로 표시해 둔 것이 있으면** 넣기 전에 다시 짚는다.

---

## STEP 6 · 올린다

**「안 올린다」를 고르셨으면 이 STEP 을 건너뛴다.** 건너뛴다고 말한다.

### 올리고 나서 주소를 말한다

```
올렸어요. 이 주소에서 열립니다.

  {주소}

이제 제가 이 주소를 받아봐서 진짜 되는지 확인하겠습니다.
```

### ★ 「올린 것」인지 확인한다

**올린 곳이 주소를 여럿 준다.** 미리보기 주소와 올린 주소가 다르다.

| | |
|---|---|
| 미리보기 주소 | **검색이 안 가져간다.** 그렇게 만들어져 있다 |
| **올린 주소** | **검색이 가져간다** |

> **★ 어느 쪽을 보고 있는지 확인한다. 헷갈리면 검사 결과가 통째로 틀린다.**

---

## STEP 7 · ★ 받아서 잰다

**05 의 심장이다.** 여기를 건너뛰면 05 를 안 한 것과 같다.

```bash
bash "{이 스킬이 설치된 폴더}/scripts/받아보기.sh" {올린 주소} --봇
```

### 검사표를 채운다

**05 가 채워서 낸다.** 빈 칸으로 내지 않는다.

| | |
|---|---|
| ○ 인 것 | 그대로 적는다 |
| **✕ 인 것** | **무엇이 ✕ 이고 왜 그런지 적는다** |
| **못 잰 것** | **「안 잼」과 왜 못 쟀는지.** 「나중에」라고 안 적는다 |

**받아본 것만 적는다.** 짐작해서 ○ 를 적지 않는다.

### 스크립트가 안 돌면

**멈추지 않는다.** 직접 받아보고 **직접 받아봤다고 `plan.md` 에 적는다.**

### 화면에는 이렇게 나간다

**✕ 만 낸다. ○ 는 개수로.**

```
받아봤어요. {N}개 중 {N}개가 ○ 입니다.

✕ 가 {N}개 나왔습니다.

  {무엇}      {왜 그런지 한 줄}
  {무엇}      {왜 그런지 한 줄}

{N}개는 제가 고칠 수 있고, {N}개는 올린 곳 설정이라 눌러주셔야 합니다.
```

**「제가 고칠 것」과 「눌러주셔야 할 것」을 갈라서 말한다.**

---

## STEP 8 · ✕ 를 고칠까

**[4/5] 이대로 고칠까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `다 고쳐줘` | `제가 고칠 수 있는 걸 다 고치고 다시 올립니다` |
| `설명 좀 더 듣고 정할래` | `✕ 마다 뭐가 문제고 안 고치면 어떻게 되는지 말씀드립니다` |
| `일단 넘어가고 나중에` | `문서에 「안 고침」으로 적어 둡니다. 07 에서 다시 봅니다` |
| `알아서 해줘` | `다 고치고 다시 올립니다` |

**「일단 넘어가고」를 고르셔도 멈추지 않는다.** 안 고친 것을 **왜 안 고쳤는지와 함께** 적는다.

### 올린 곳 설정 때문인 것은 클릭 순서를 적어준다

**로봇이 막히거나 검색 금지 표시가 붙은 것은 대개 올린 곳 설정이다.**
**05 가 못 고친다. 그래도 「설정에서 바꾸세요」로 끝내지 않는다.**

```
{어디}
  1. {어디를 누르나}
  2. {어디를 누르나}
  3. {무엇을 끄나}

바꾸시고 알려주시면 다시 받아보겠습니다.
```

---

## STEP 9 · 주소를 지금 살까

**02 에서 이름을 정해 놨다.** 사는 건 여기서 한다.

### ★ 겁을 줘서 사게 하지 않는다

**「공짜 주소는 검색에 안 나온다」고 말하지 않는다. 사실이 아니다.**
[`references/근거와한계.md`](references/근거와한계.md) 3장에 있다.

```
02 에서 사이트 주소 이름을 정해 두셨어요.

지금 안 사셔도 됩니다. 지금 주소로도 검색에 나옵니다.
다만 나중에 옮기면 그동안 걸린 링크가 다 죽습니다.

  1. 나중에 옮기면 쌓인 게 처음부터
  2. 계정을 묶는 중심이 남의 주소 아래면 약합니다
  3. 프로젝트 이름을 바꾸면 주소가 바뀝니다

연 2만원쯤 합니다.
```

**[5/5] 사이트 주소를 지금 살까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `지금 산다` | `사는 순서와 잇는 법을 알려드립니다. 사고 오시면 제가 잇습니다` |
| `나중에 할래` | `지금 주소로 갑니다. 문서에 「안 정함」으로 적어 둡니다` |
| `이미 있다` | `주소를 알려주시면 잇겠습니다` |
| `잘 모르겠다` | `나중에 하셔도 됩니다. 지금 주소로 갑니다` |

### 산다고 하시면

**사는 순서와 잇는 순서를 클릭으로 적어준다.**
**02 가 비었는지 확인해 둔 후보를 그대로 낸다.** 다시 확인하지 않는다.

**사고 오시면 서술로 받아서 잇는다. 클릭에 안 센다.**

### 잇고 나면 다시 받아본다

**주소를 바꾸면 검사 결과가 달라진다.** STEP 7 을 그 주소로 한 번 더 돈다.

---

## STEP 10 · 관리 화면을 짓는다

**04 가 뼈대를 그려 뒀다. 05 가 동작까지 짓는다.**

```
이제 관리 화면을 붙이겠습니다.
```

### 어디까지 짓나

| | 05 가 짓나 |
|---|---|
| 화면이 보이나 | **짓는다** |
| 들어가는 자리를 막기 | **짓는다** |
| 글 목록 · 상태 바꾸기 · 켜고 끄기 | **짓는다** |
| 갈래 편집 | **짓는다** |
| **글마다 제목 · 요약 · 답하는 질문 · 그림글을 넣는 칸** | ★ **짓는다.** 아래 |
| 지표 화면의 실제 숫자 | **안 짓는다.** 08 |
| **그 칸을 실제로 채우는 것** | **안 한다.** 06 |

### ★ 그림글 넣는 칸을 반드시 만든다

**제목과 요약은 규칙만 있으면 자동으로 채워진다. 그림글은 아니다.**
**그림마다 사람이 봐야 한다.**

> **칸이 없으면 06 이 그림글을 넣을 자리가 없다.**
> **그러면 그림이 안 보이는 사람에게 아무것도 안 남는다.**

### ★ 셋을 다 막는다

| | |
|---|---|
| 1 | **기계가 읽는 표시를 안 붙인다** |
| 2 | **로봇을 안 들인다** |
| 3 | **사이트맵에 안 넣는다** |

**하나라도 빠지면 관리 화면이 검색에 뜬다.**

> **로봇 파일에 관리 화면 주소를 적는 것 자체가 그 주소를 알려주는 일이다.**
> **화면 자체에 「싣지 마라」 표시를 같이 단다.**

### 들어가는 자리를 막는다

**막는 방법은 지은 방식에 따라 다르다.** [`references/짓는규칙.md`](references/짓는규칙.md) 4장을 본다.

| | |
|---|---|
| **열쇠를 코드에 안 박는다** | 올리면 그대로 공개된다 |
| **밖에 안 보이는 자리에 둔다** | 그 자리가 어디인지 `지은것.md` 에 적는다 |
| 어떻게 들어가는지 | **사람에게 알려준다.** 안 알려주면 못 들어간다 |

---

## STEP 11 · 다시 올리고 다시 받아본다

**관리 화면을 붙였으니 다시 올리고 다시 잰다.**

| | |
|---|---|
| 1 | 다시 올린다 |
| 2 | **`받아보기.sh` 를 다시 돌린다** |
| 3 | ★ **관리 화면 주소도 받아본다.** 막혔는지 확인 |
| 4 | ★ **관리 화면 안이 그려지는지 본다** |
| 5 | ★ **저장 · 사진 올리기를 실제로 시켜본다** |
| 6 | **틀마다 실물 하나로 열어본다** |
| 7 | ★ **화면 문구가 지금 구조와 맞는지 읽어본다** |

### 관리 화면이 진짜 막혔는지 본다

**추측하지 않는다.** 관리 화면 주소를 받아보고 **로봇 이름으로도 받아본다.**

### ★ 막힌 것 말고 「안이 그려지나」를 따로 본다

**막혔는지만 보고 통과시키면 안이 깨져 있어도 모른다. 1회차에 그렇게 놓쳤다.**

로그인 값을 비우면 이 컴퓨터에서는 관리 화면이 그냥 열리게 지어 두었다.
**그 상태로 띄워서 화면마다 제목이 나오는지까지 본다.**

| 안 되는 것 | 되는 것 |
|---|---|
| `200` 이니 통과 | 제목 글자가 실제로 오는지 본다 |
| 짓기가 통과했으니 통과 | **짓기와 타입 검사는 이 오류를 못 잡는다** |

### ★ 저장을 실제로 시켜본다

**화면이 그려지는 것과 저장이 되는 것은 다르다.**

| | 무엇 | 어떻게 |
|---|---|---|
| 1 | 담기나 | 지금 값을 그대로 다시 저장해 본다. 아무것도 안 바뀐다 |
| 2 | 담을 데에 실제로 들어갔나 | **담을 데를 열어서 확인한다.** 「저장됐습니다」 문구를 안 믿는다 |
| 3 | 앞 화면에 반영되나 | 값을 하나 바꾸고 **앞 화면을 받아본다.** 바꾼 건 되돌린다 |
| 4 | 사진이 손님에게 가나 | 올리고 **로그인 없이 그 주소를 받아본다.** 시험 사진은 지운다 |

**3번을 안 하면 「담기는데 안 보이는」 상태를 못 잡는다.** 1회차에 이걸 두 번 놓쳤다.

### ★ 화면 문구를 소리 내어 읽어본다

**구조를 바꿨으면 그 구조를 설명하는 문구가 아직 사실인지 본다.**

「다시 올려야 합니다」 · 「이 컴퓨터에서만 됩니다」 · 「나중에 됩니다」.
**이런 말이 남아 있으면 저장이 멀쩡해도 사용자는 고장으로 읽는다.**

### 틀마다 실물 하나

**글 화면 틀에 넣어볼 글이 0편이면 임시로 하나 만들어 열어본다.**
**열어보고 지운다. 지웠다고 적는다.**

### ✕ 가 0이 될 때까지

**안 되면 안 된다고 적는다.** 「전부 ○」를 만들려고 검사를 느슨하게 하지 않는다.

---

## STEP 12 · 문서 쓰기 · 마무리

**여기가 마지막이다.** 아래를 **한 덩어리로** 낸다.

쓰기 전에 한 줄 알리고 **답은 안 기다린다.**

```
정리해서 문서로 쓸게요.
```

양식은 [`references/산출물양식.md`](references/산출물양식.md) 에 있다.

### plan.md

```markdown
# {표시 이름} · 지금 어디고 다음에 뭘 하나

{날짜} · 05 BUILD · 화면 {N}종류 · 검사 {N}항목 중 ○ {N}개

## 사이트
{주소}

## 정해진 것
| | 정해짐 | 어디에 |
|---|---|---|
| 무엇으로 지었나 | ○ | 지은것.md |
| 어디에 올렸나 | ○ | 지은것.md |
| 주소 규칙 | ○ | 지은것.md |
| 화면 {N}종류 | ○ | 사이트 |
| 기계가 읽는 표시 | ○ | 사이트 · 검사표.md |
| 로봇 · 사이트맵 · llms.txt | ○ | 사이트 · 검사표.md |
| 관리 화면 · 막기 | ○ | 지은것.md |
| **받아본 결과** | ○ | **검사표.md** |

## 안 정해진 것
| 무엇 | 왜 비었나 | 언제 정하면 되나 |
|---|---|---|
| 글 {N}편 | **아직 안 쓰인 글이다** | 06 PUBLISH |
| {그 밖} | {왜} | {언제} |

## 받아본 결과
| | 개수 |
|---|---|
| ○ | {N} |
| **✕ · 고침** | {N} |
| **✕ · 안 고침** | {N} ← 아래 |

### 안 고친 것
| 무엇 | 왜 | 어디서 하나 |
|---|---|---|
| {무엇} | {왜} | {어디} |

## 깐 것
{지은것.md 를 보라고 한 줄}

## 다음에 할 일
1. {사람이 할 일}

## 다음 단계로 넘길 것
| 어디로 | 무엇 |
|---|---|
| 06 PUBLISH | 선 사이트 · 관리 화면 들어가는 법 · 글이 들어갈 자리 · **검사표.md ★ 시작할 때 다시 재 주세요** |
| 07 TECH | **검사표.md.** 도구로 다시 잴 기준이 된다 |
| 08 MEASURE | 근거 없이 넣은 것 목록 |

## 06 으로 가려면
git clone https://github.com/iny101400-young/iny-publish.git
cd iny-publish && bash install.sh
```

### 다 쓰고 나서 · 여기서 멈추지 않는다

**아래를 이어서 바로 낸다.**

먼저 `plan.md` 내용을 채팅창에 그대로 한 번 보여준다.

**그다음 사이트 주소와 관리 화면 들어가는 법을 말한다.**

```
사이트는 여기서 열립니다.

  {주소}

관리 화면은 여기고, 들어가는 법은 지은것.md 에 적어 뒀습니다.

  {관리 화면 주소}
```

**절대경로를 문서에 안 적는다.** 화면에 말할 때만 실제 경로를 쓴다.

그다음 답을 못 받은 질문이 있으면 **반드시 말한다.**

그리고 한 번 더 확인시킨다.

| | |
|---|---|
| 사이트 | 화면 {N}종류가 실제로 열립니다 |
| **받아보기** | **제가 그 주소를 받아봐서 쟀습니다.** {N}개 중 ○ {N}개 |
| 관리 화면 | **막혀 있습니다.** 받아봐서 확인했습니다 |
| 글 | **0편입니다.** 06 에서 씁니다 |
| 깐 것 | **{N}개.** 지우는 법은 `지은것.md` 에 있습니다 |
| 주소 | {산 것 / 아직 안 산 것} |

마지막으로 **링크와 설치 명령까지** 준다.

```
다음은 06 PUBLISH 입니다.
방금 선 사이트에 첫 글을 내는 단계입니다.
글을 쓰고, 말투를 다듬고, 세 가지를 검사한 뒤 발행합니다.

  https://github.com/iny101400-young/iny-publish

받으시려면 이 폴더에서 아래를 그대로 치시면 됩니다.

  git clone https://github.com/iny101400-young/iny-publish.git
  cd iny-publish
  bash install.sh

깔고 나서 Claude Code 를 다시 열고 "첫 글 쓰기" 라고 치세요.
```

**같은 링크를 `plan.md` 에도 적는다.**

**그리고 맨 끝에 이 한 줄로 닫는다.**

```
쭉 보시고 고칠 데만 말씀해 주세요.
```

**이 줄 뒤에 아무것도 두지 않는다.**

---

## 05 가 정하는 것과 안 정하는 것

| | 05 가 정하나 | 왜 |
|---|---|---|
| 무엇으로 지을까 | **정한다** | 고를 근거가 없는 걸 묻지 않는다 |
| 주소 규칙 · 한글 주소 | **정한다** | 03 이 후보를 냈다 |
| 어디에 올릴까 | **묻는다** | 계정이 필요하다 |
| 화면을 실제로 짓기 | **정한다** | 이게 05 의 일이다 |
| 표시를 **붙이기** | **정한다** | **종류는 04 가 정했다** |
| 로봇 · 사이트맵 · `llms.txt` | **정한다** | |
| 04 가 캐둔 것 채우기 | **정한다** | 내용이 이미 있다 |
| 관리 화면 **동작까지** · 막기 | **정한다** | |
| 사이트 주소를 사고 잇기 | **묻는다** | 결제가 필요하다. **안 해도 된다** |
| **받아보고 검사표를 채우기** | **정한다** | **05 의 심장이다** |
| **글 본문** | **안 정한다** | **06** |
| **첫 글 발행 · 게이트 검사** | **안 정한다** | **06** |
| **그림글의 실제 문구** | **안 정한다** | **06.** 05 는 넣는 칸까지 |
| **감사 도구를 돌리기** | **안 정한다** | **07** |
| **지표 · 검색 콘솔** | **안 정한다** | **08** |
| 화면을 더하거나 요소를 바꾸기 | **안 정한다** | **04** |

---

## 지켜야 할 것

| | |
|---|---|
| 순서 | 내부 상태를 첫 화면에 내지 않는다. **안내문이 먼저다** |
| **앞 화면 먼저** | **앞 화면을 세워 올리고 재고, 그다음 관리 화면.** 중간에 끊겨도 사이트가 선다 |
| **받아보기** | **파일만 보고 통과시키지 않는다.** 이게 05 의 심장이다 |
| **짐작 금지** | **받아본 것만 적는다.** 못 쟀으면 못 쟀다고 |
| **검사표** | **05 가 채워서 낸다.** 다만 **자기 채점이라 칸이 셋이다.** 06 과 07 이 다시 잰다 |
| **색** | **`근거.html` 에서만.** 화면설계서 파일에는 문서 색이 섞여 있다 |
| **없는 복잡함** | **`API` 가 「없음」이면 그 자리를 안 만든다** |
| **깐 것** | **무엇을 왜 깔고 어떻게 지우는지 적는다.** 「안 깝니다」를 못 쓴다 |
| **관리 화면** | **표시 · 로봇 · 사이트맵 셋을 다 막고, 받아봐서 확인한다** |
| **그림글 칸** | **관리 화면에 반드시 만든다.** 자동으로 못 만드는 유일한 것이다 |
| **틀마다 실물 하나** | **열어보고 지운다. 지웠다고 적는다** |
| **공짜 주소** | **「검색에 안 나온다」고 말하지 않는다.** 겁을 줘서 사게 하지 않는다 |
| **주소** | **한 번 정하면 안 바꾼다** |
| **개인정보** | **연락처와 계정 주소가 코드에 박힌다.** 넣기 전에 짚는다 |
| **열쇠** | **코드에 안 박는다** |
| 04 산출물 | **안 고친다** |
| 06·07 의 일 | **미리 하지 않는다** |
| 개수 | **미리 안 박는다.** 다 짓고 나서 말한다 |
| 클릭 | **다섯 개.** 한 번에 하나씩 |
| 손일 | **셋만 사람이 한다.** 올릴 곳 계정 · 담을 데 계정 · 주소 사기. **클릭 순서를 적어준다** |
| 출력 | **파일 목록과 오류 원문을 화면에 쏟지 않는다** |
| 「없음」 | 빈칸으로 두지 않는다 |
| 「나중에」 | 산출물에 안 쓴다. **왜 못 했는지**를 쓴다 |
| 자동 커밋 | 스킬이 알아서 커밋하지 않는다 |
| 꼬리 | **STEP 12 가 마지막이다** |
| 시간 | **숫자로 약속하지 않는다** |
| 경로 | 이 파일에 적어두지 않는다 |
